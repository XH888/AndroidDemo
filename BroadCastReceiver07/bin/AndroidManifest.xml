<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.broadcastreceiver07"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />
    
	<permission android:name="com.example.receiver.PERMISSION"/>
	<uses-permission android:name="com.example.receiver.PERMISSION"/>
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.broadcastreceiver07.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <receiver android:name="com.example.receiver.FirstReceiver">
            <intent-filter android:priority="100">
                <action android:name="com.example.receiver.BROADCASTRECEIVER"/>
            </intent-filter>
        </receiver>
        <receiver android:name="com.example.receiver.SecondReceiver">
            <intent-filter android:priority="99">
                <action android:name="com.example.receiver.BROADCASTRECEIVER"/>
            </intent-filter>
        </receiver>
        <receiver android:name="com.example.receiver.ThirdReceiver">
            <intent-filter android:priority="98">
                <action android:name="com.example.receiver.BROADCASTRECEIVER"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>
